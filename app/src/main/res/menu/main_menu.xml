<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/group_list"
        android:title="List of groups"
        android:visible="false"/>

    <item android:id="@+id/results"
        android:title="Results"
        android:visible="false"/>

    <item android:id="@+id/add_group"
        android:title="Add group"
        android:visible="false"/>

    <item android:id="@+id/set_visibility"
        android:title="Set task visibility"
        android:visible="false"/>

</menu>